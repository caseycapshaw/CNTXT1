# CLAUDE.md — {{NAME}}'s Knowledge Base

@AGENTS.md

The session instructions live in `AGENTS.md` (the cross-vendor standard, imported
above — Grok Build and other agents read it natively); the KB schema is
`SYSTEM/SCHEMA.md`. Nothing Claude-specific lives here — keep it that way so the
two files can never drift.
