[project]
name = "cntxt1"
version = "0.1.0"
description = "CNTXT1 — LLM-maintained knowledge-base starter kit (schema/validation tooling)"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7",
    "pydantic-yaml>=1.5",
    "pyyaml>=6.0",
]
