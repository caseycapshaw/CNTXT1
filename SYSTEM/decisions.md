# Decisions — rulings ledger

{{NAME}}'s explicit rulings, one line each, newest last. This is the **second
ledger**: `SYSTEM/log.md` records *what changed* (every meaningful update);
this file records *what was decided* (only explicit rulings). Two ledgers,
two grains — don't merge them.

**The three disciplines:**

1. **Index, not record.** One entry = date + short title + one–two sentences +
   a pointer to the fuller record (the initiative/concept/raw note that holds
   the reasoning). Never write rationale here that the record lacks; on
   conflict, the record wins.
2. **Explicit trigger.** A line lands only on {{NAME}}'s explicit ruling-verb —
   "decided", "ruled", "adopted", "that's a decision", "record decision", and
   kin (their vocabulary, an open family) — or on their confirmation of a
   close-ritual-identified ruling. **Unattended jobs and sessions never
   append here**; an LLM-identified ruling is a proposal until {{NAME}}'s yes.
3. **Non-exhaustive by declaration.** This ledger is not complete; "not in
   the ledger" never means "not ruled." Older decisions live where they were
   made (per-note `## Decisions` sections) and migrate here only if touched
   again.

---

_No entries yet. The first ruling {{NAME}} makes with an explicit ruling-verb
lands here as a one-liner pointing at its fuller record._
